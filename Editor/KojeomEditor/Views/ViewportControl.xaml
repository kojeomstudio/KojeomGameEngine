<UserControl x:Class="KojeomEditor.Views.ViewportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Viewport" FontWeight="Bold" FontSize="12"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Background="#0E0E0E">
            <Viewbox Stretch="Uniform" Width="200" Height="100">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="🎮 3D Viewport" FontSize="24" HorizontalAlignment="Center" Foreground="#666"/>
                    <TextBlock Text="Engine rendering will appear here" FontSize="12" HorizontalAlignment="Center" Foreground="#444" Margin="0,10,0,0"/>
                </StackPanel>
            </Viewbox>

            <Canvas>
                <TextBlock Text="Perspective" Foreground="#888" FontSize="10" Canvas.Top="8" Canvas.Left="8"/>
                <TextBlock Text="Lit" Foreground="#888" FontSize="10" Canvas.Top="8" Canvas.Right="8" HorizontalAlignment="Right"/>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
