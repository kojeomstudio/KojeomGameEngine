<UserControl x:Class="KojeomEditor.Views.ContentBrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Content Browser" FontWeight="Bold" FontSize="12"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Import" Padding="4,2" Margin="0,0,4,0" Click="Import_Click"/>
                    <Button Content="Refresh" Padding="4,2" Click="Refresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#252526" Padding="4">
            <StackPanel Orientation="Horizontal">
                <Button Content="Assets" Padding="8,2" Margin="0,0,2,0" Click="NavigateToAssets_Click"/>
                <TextBlock Text=">" VerticalAlignment="Center" Margin="4,0" Foreground="#999"/>
                <ContentControl Content="{Binding CurrentFolder}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Background="#1E1E1E">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TreeView Grid.Column="0" ItemsSource="{Binding Folders}" 
                      SelectedItemChanged="FolderTree_SelectedItemChanged"
                      Background="#252526" BorderThickness="0">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding SubFolders}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📁" Margin="0,0,4,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="2" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#3F3F46"/>

            <ListBox Grid.Column="2" ItemsSource="{Binding Assets}" 
                     SelectedItem="{Binding SelectedAsset}"
                     Background="#1E1E1E" BorderThickness="0"
                     SelectionMode="Single" MouseDoubleClick="Asset_DoubleClick">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="80" Height="90" Margin="4" Background="#2D2D30" 
                                CornerRadius="4" Cursor="Hand">
                            <StackPanel>
                                <Border Width="64" Height="64" Margin="4,4,4,2" Background="#1E1E1E" CornerRadius="2">
                                    <TextBlock Text="{Binding IconText}" FontSize="28" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding Name}" FontSize="10" 
                                           TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"
                                           HorizontalAlignment="Center" Margin="2,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
