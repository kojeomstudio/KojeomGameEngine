<UserControl x:Class="KojeomEditor.Views.MaterialEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:KojeomEditor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Material Editor" FontWeight="Bold" FontSize="12"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="8,8,8,0" Orientation="Horizontal">
            <ComboBox x:Name="MaterialPresetComboBox" Width="150" SelectedIndex="0" SelectionChanged="OnMaterialPresetSelectionChanged">
                <ComboBoxItem Content="Default"/>
                <ComboBoxItem Content="Metal"/>
                <ComboBoxItem Content="Plastic"/>
                <ComboBoxItem Content="Rubber"/>
                <ComboBoxItem Content="Gold"/>
                <ComboBoxItem Content="Silver"/>
                <ComboBoxItem Content="Copper"/>
                <ComboBoxItem Content="Emissive"/>
            </ComboBox>
            <Button Content="New" Margin="8,0,0,0" Padding="8,2" Click="OnNewMaterial"/>
            <Button Content="Open" Margin="4,0,0,0" Padding="8,2" Click="OnLoadMaterial"/>
            <Button Content="Save" Margin="4,0,0,0" Padding="8,2" Click="OnSaveMaterial"/>
            <Button Content="Save As" Margin="4,0,0,0" Padding="8,2" Click="OnSaveMaterialAs"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="8" DataContext="{Binding MaterialViewModel}">
                <TextBlock Text="Material Name:" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Albedo Color" FontWeight="Bold" FontSize="11" Margin="0,8,0,4"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,2,0">
                        <TextBlock Text="R" Foreground="#F44" FontSize="9"/>
                        <TextBox Text="{Binding AlbedoR, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="2,0">
                        <TextBlock Text="G" Foreground="#4F4" FontSize="9"/>
                        <TextBox Text="{Binding AlbedoG, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="2,0">
                        <TextBlock Text="B" Foreground="#44F" FontSize="9"/>
                        <TextBox Text="{Binding AlbedoB, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" Margin="2,0,0,0">
                        <TextBlock Text="A" Foreground="#888" FontSize="9"/>
                        <TextBox Text="{Binding AlbedoA, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Albedo Texture:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding AlbedoTexturePath}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Content="..." Width="30" Margin="4,0,0,0" Click="OnBrowseAlbedoTexture"/>
                </Grid>

                <TextBlock Text="PBR Properties" FontWeight="Bold" FontSize="11" Margin="0,16,0,4"/>

                <TextBlock Text="Metallic:" Foreground="#999" FontSize="10" Margin="0,4,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Value="{Binding Metallic}" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"/>
                    <TextBlock Grid.Column="1" Text="{Binding Metallic, StringFormat=F2}" Width="40" TextAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock Text="Roughness:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Value="{Binding Roughness}" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"/>
                    <TextBlock Grid.Column="1" Text="{Binding Roughness, StringFormat=F2}" Width="40" TextAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock Text="Ambient Occlusion:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Value="{Binding AO}" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"/>
                    <TextBlock Grid.Column="1" Text="{Binding AO, StringFormat=F2}" Width="40" TextAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock Text="Emissive Color" FontWeight="Bold" FontSize="11" Margin="0,8,0,4"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,2,0">
                        <TextBlock Text="R" Foreground="#F44" FontSize="9"/>
                        <TextBox Text="{Binding EmissiveR, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="2,0">
                        <TextBlock Text="G" Foreground="#4F4" FontSize="9"/>
                        <TextBox Text="{Binding EmissiveG, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="2,0,0,0">
                        <TextBlock Text="B" Foreground="#44F" FontSize="9"/>
                        <TextBox Text="{Binding EmissiveB, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" Padding="2"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Emissive Intensity:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0" Value="{Binding EmissiveIntensity}" Minimum="0" Maximum="10" SmallChange="0.1" LargeChange="1"/>
                    <TextBlock Grid.Column="1" Text="{Binding EmissiveIntensity, StringFormat=F1}" Width="40" TextAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock Text="Texture Maps" FontWeight="Bold" FontSize="11" Margin="0,16,0,4"/>

                <TextBlock Text="Normal Map:" Foreground="#999" FontSize="10" Margin="0,4,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding NormalTexturePath}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Content="..." Width="30" Margin="4,0,0,0" Click="OnBrowseNormalTexture"/>
                </Grid>

                <TextBlock Text="Metallic Map:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding MetallicTexturePath}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Content="..." Width="30" Margin="4,0,0,0" Click="OnBrowseMetallicTexture"/>
                </Grid>

                <TextBlock Text="Roughness Map:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding RoughnessTexturePath}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Content="..." Width="30" Margin="4,0,0,0" Click="OnBrowseRoughnessTexture"/>
                </Grid>

                <TextBlock Text="AO Map:" Foreground="#999" FontSize="10" Margin="0,8,0,2"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding AOTexturePath}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Content="..." Width="30" Margin="4,0,0,0" Click="OnBrowseAOTexture"/>
                </Grid>

                <TextBlock Text="Presets" FontWeight="Bold" FontSize="11" Margin="0,16,0,4"/>
                <WrapPanel>
                    <Button Content="Default" Padding="8,4" Margin="0,0,4,4" Click="OnPresetDefault"/>
                    <Button Content="Metal" Padding="8,4" Margin="0,0,4,4" Click="OnPresetMetal"/>
                    <Button Content="Plastic" Padding="8,4" Margin="0,0,4,4" Click="OnPresetPlastic"/>
                    <Button Content="Rubber" Padding="8,4" Margin="0,0,4,4" Click="OnPresetRubber"/>
                    <Button Content="Gold" Padding="8,4" Margin="0,0,4,4" Click="OnPresetGold"/>
                    <Button Content="Silver" Padding="8,4" Margin="0,0,4,4" Click="OnPresetSilver"/>
                    <Button Content="Copper" Padding="8,4" Margin="0,0,4,4" Click="OnPresetCopper"/>
                    <Button Content="Emissive" Padding="8,4" Margin="0,0,4,4" Click="OnPresetEmissive"/>
                </WrapPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
